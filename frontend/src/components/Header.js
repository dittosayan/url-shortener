import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="header">
            <span className="header-text">
                <p>URL Shortener</p>
            </span>
        </div>
    );
}

export default Header;
